/**
 * Local declarations for the MV3 surfaces Chrome's own typings still miss.
 *
 * Empty on purpose at this stage: nothing untyped is used yet. The file exists so a missing
 * surface has an obvious home instead of being papered over with `as any` at the call site.
 *
 * @see https://wxt.dev — the official WXT example keeps the same file for the same reason.
 */

export {};
